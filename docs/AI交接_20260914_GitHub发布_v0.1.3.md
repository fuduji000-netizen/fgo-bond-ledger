# AI 交接：GitHub 发布 v0.1.3（2026-09-14）

## 本次修改与发布准备

- 在已批准的 `codex/quantplatform-core-rebuild` 分支提交当前账本版本，提交为 `45d42a1`。
- 将 `package.json` 与 `package-lock.json` 版本从 `0.1.2` 升至 `0.1.3`。
- 生成 Windows 发布包：
  - `C:\Users\fuduj\Documents\ChatGPT\fgo羁绊计算器\release\fgo-bond-ledger-0.1.3.exe`
  - `C:\Users\fuduj\Documents\ChatGPT\fgo羁绊计算器\release\fgo-bond-ledger-0.1.3.exe.blockmap`
  - `C:\Users\fuduj\Documents\ChatGPT\fgo羁绊计算器\release\latest.yml`
- `latest.yml` 的版本为 `0.1.3`，安装包 SHA-512 与清单一致。
- 已执行并通过：
  - `npm run build:standalone`
  - `npm test`：71 项全部通过
  - `node --check src/app.js`
  - `node --check src/bbchannel.js`
  - `node --check src/app.standalone.js`
  - `npm run dist:win`
  - 暂存区 `git diff --check`
- 已推送分支 `origin/codex/quantplatform-core-rebuild`。
- 已推送标签 `v0.1.3`。

## 当前未完成项

- GitHub Release `v0.1.3` 尚未创建，三个发布文件尚未上传到 Release。
- 通过已授权 Chrome 控制连接 GitHub 时，浏览器控制端返回 `Codex auth token is unavailable`；已确认 Chrome、扩展和 Native Messaging 配置均存在并启用，也已按授权重新打开 Chrome 窗口，重试仍失败。
- GitHub 当前公开的 latest Release 仍为 `v0.1.2`，因此旧版账本暂时还不能通过更新地址发现 `v0.1.3`。

## 不变更范围

- 未运行安装程序。
- 未覆盖 `E:\迦勒底羁绊账本`。
- 未写入 BBC 文件、账号信息或其他本地软件配置。
