# AI交接：删除 BBC 礼装制作与导出礼装信息

日期：2026-09-13

## 本次实际修改

- 删除账本界面的“制作 BBC 礼装模板”入口及完整制作对话框。
- 删除 BBC 礼装模板生成模块、Wiki 原图获取、模板图片直接导入 BBC 礼装目录相关的前端逻辑、桌面端 IPC 和主进程处理函数。
- 保留普通“打开 BBchannel”对 `.cmd` 启动文件的支持；BBchannel 路径选择仍可选择 `.exe` 或 `.cmd`，`.cmd` 继续通过命令解释器启动。
- 修改 `src/bbchannel.js` 的队伍配置生成：
  - 保留六个从者位置、`assistIdx`、`usedServant` 和连接默认字段；
  - 删除 `assistMode`、`assistEquip`、`master_equip`；
  - 好友位没有礼装或礼装不在 BBC 模板中的情况下不再阻止导出。
- 更新相关测试，并重新生成 `src/app.standalone.js`。

## 实际验证结果

- `npm run build:standalone`：通过。
- `npm test`：通过，62 项全部通过。
- `npm run dist:win`：通过，生成 Windows 未打包目录和 NSIS 安装包。
- 已将新的 `release/win-unpacked/resources/app.asar` 覆盖到：
  `E:\迦勒底羁绊账本\fgo-bond-ledger\resources\app.asar`
- 覆盖前确认账本进程已退出；源文件与目标文件 SHA-256 均为：
  `A80C4848A720FE420B9D68901C4E39F2CE0235CF7EB707873A5ABF553D52A88B`
- 使用 asar 列表确认部署包包含新的 `index.html`、`src/app.standalone.js` 和 `desktop/preload.cjs`，不再包含 `src/bbc-template-maker.js`。

## 未完成项

- 未操作 `E:\BBchannel\BBchannel`，也未读取或写入 BBC 配置正文。
- 未提交 Git commit；当前仓库仍按用户现有工作区状态保留。
