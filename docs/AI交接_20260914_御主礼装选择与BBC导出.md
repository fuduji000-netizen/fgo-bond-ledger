# AI 交接：御主礼装选择与 BBC 导出

## 本次修改

- 在战斗设定中加入“御主礼装（BBC）”选择框，显示 BBC `master_info.json` 的 SN 0–26 对应礼装名称。
- 新增状态字段 `masterEquip`，默认值为 0；读取旧存档时也兼容旧的 `master_equip` 字段，并将非法编号归一化为 0 或合法回退值。
- 导出 BBchannel 队伍配置时写入 `master_equip`，写入值为所选礼装的 SN 序号，不写入礼装名称。
- 保留 BBC 队伍配置不导出编队内羁绊礼装的现有规则。
- 重新生成 `src/app.standalone.js`，并递增独立入口脚本查询版本号；为编号提示补充样式。
- 为默认值、合法编号、非法编号回退和独立入口 DOM 增加测试。

## 验证结果

已在当前 checkout 执行并通过：

- `node --check src/app.js`
- `node --check src/bbchannel.js`
- `node --check src/app.standalone.js`
- `npm run build:standalone`
- `npm test`：71 项全部通过
- `git diff --check`

## 未完成项

- 尚未提交、推送或制作 GitHub 发布包。
- 尚未把当前 checkout 覆盖复制到 `E:\迦勒底羁绊账本`；本次只修改当前项目工作区。
- 未写入 BBC 的 `scripts_settings.json` 或其他 BBC 配置文件。
