# AI交接：GitHub仓库与在线更新地址待绑定

日期：2026-09-14

## 本次实际修改

- 当前本地工作分支切换为 `codex/quantplatform-core-rebuild`，用于后续仓库整理。
- 新增根目录 `.gitignore`，忽略 `node_modules`、本地 `release` 构建产物、临时检查目录、`.asar` 和日志，避免把本机环境与构建缓存导入 GitHub。
- 暂未写入真实 GitHub 更新地址，`desktop/update-config.json` 仍保持空地址：
  ```json
  { "url": "", "channel": "latest" }
  ```

## 当前事实

- 本地仓库尚无 Git commit，也没有 remote。
- 无法在不知道 GitHub 账户名、仓库名且当前环境没有 GitHub 登录凭据的情况下创建真实仓库 URL或向 GitHub 发布内容。
- 可使用 GitHub 的新建仓库入口创建空仓库；创建完成后需要拿到实际仓库地址，例如：
  `https://github.com/<用户名>/<仓库名>`。
- GitHub Release 通用更新地址应绑定为该仓库的：
  `https://github.com/<用户名>/<仓库名>/releases/latest/download`

## 待用户提供/完成

1. 在 GitHub 创建空仓库，或直接提供已有仓库 URL。
2. 用户将本地项目导入该仓库后，提供最终仓库 URL。
3. 再把该 URL 写入 `desktop/update-config.json`，重新构建并验证 `latest.yml`、安装包和 `.blockmap` 的 Release 资产。

## 已有更新功能状态

- 桌面版启动时只自动检查一次更新。
- 没有新版本时不弹窗；有新版本时才显示更新提示。
- `npm run dist:win` 会生成安装包、`.blockmap` 和 `release/latest.yml`。

## 未完成项

- 未创建真实 GitHub 仓库、未配置 remote、未上传代码、未发布 Release。
- 未绑定真实更新地址；等待用户提供 GitHub 仓库 URL。上述外部发布和 Git 收口动作需在用户明确指定目标地址后执行。
