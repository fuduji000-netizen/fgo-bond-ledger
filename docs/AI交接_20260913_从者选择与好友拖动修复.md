# AI交接：从者选择器完整显示与好友位拖动范围修复

日期：2026-09-13

## 本次实际修改

- 移除普通模式从者选择器结果的 `slice(0, 120)` 截断；职阶、星级和搜索筛选现在会显示所有匹配从者。
- 好友位卡片新增整卡拖动面：除从者选择区域和礼装区域外，标题、属性、冠位设置、等级/状态字段、单场结果等区域均可拖动好友位。
- 从者头像区域继续执行原有的从者位置交换；礼装区域继续执行原有的礼装交换，不会被好友位拖动抢占。
- 更新独立版构建文件及对应静态测试。

## 验证结果

- `npm run build:standalone`：通过。
- `npm test`：通过，63 / 63 项通过。
- `npx electron-builder --win dir --x64`：通过，已生成新的 `release/win-unpacked`。
- 新构建已覆盖到：
  `E:\迦勒底羁绊账本\fgo-bond-ledger\resources\app.asar`
- 构建包与目标包 SHA-256 均为：
  `7257C7F15A1EEF2981708FB967F0062DF726CE67C3188F01666A403C6BA7EA38`

## 未完成项

- 无。
