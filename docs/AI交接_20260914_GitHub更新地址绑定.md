# GitHub 更新地址绑定交接记录（2026年9月14日）

## 本次修改

- 将 Git 远程 `origin` 绑定到 `https://github.com/fuduji000-netizen/fgo-bond-ledger.git`。
- 将桌面版更新源配置为 `https://github.com/fuduji000-netizen/fgo-bond-ledger/releases/latest/download`，通道保持 `latest`。
- 更新 README 中的在线更新发布地址与发布说明。

## 验证结果

- 已读取并确认当前分支为 `codex/quantplatform-core-rebuild`。
- 已确认 `desktop/update-config.json` 为有效 JSON，更新地址为 HTTPS。
- 尚未向 GitHub 推送代码、创建 Release 或上传安装包；这些是对外发布/最终 Git 收口动作，本次未执行。

## 未完成项

- 需要在同一次 `npm run dist:win` 构建后，将 `release/latest.yml`、NSIS 安装包和 `.blockmap` 一起上传到 GitHub Release，在线更新检测才会有可用的远端版本。
