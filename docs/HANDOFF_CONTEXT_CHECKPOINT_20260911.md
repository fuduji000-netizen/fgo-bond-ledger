# 2026-09-11 交接检查点：编队 Cost、礼装推荐与礼装缩略图

## 当前状态

用户提出的四项问题已在工作区源码和新生成的 Windows 安装包中复核：

1. 编队 Cost 改为统一规则：好友位不计入；普通本方位置计算从者与常规礼装；冠位从者与其冠位羁绊礼装不占用 Cost。
2. 已移除“计入好友 Cost”的旧设置和对应逻辑。
3. 推荐按钮名称为“牵绊最大化礼装推荐”；其点击事件已连接到礼装最大化求解器，方案可在面板中应用。
4. 编队礼装以方形缩略图显示，图片裁切定位在下半部，并显示满破星标。

## 关键文件

- `src/calculator.js`：统一的编队单位置 Cost 计算。
- `src/recommendation.js`：在 Cost、持有礼装、冠位位置与加成规则下搜索最大化方案。
- `src/app.js`：Cost 汇总、推荐按钮事件、推荐方案应用与编队礼装渲染。
- `src/app.standalone.js`：由源码重新生成，桌面程序实际加载。
- `index.html`：推荐按钮文本与推荐结果面板。
- `styles.css`：礼装缩略图与满破星标样式。
- `tests/`：51 项自动化测试。

## 已完成验证

- `npm run build:standalone`：成功。
- `npm test`：51 项通过，0 项失败。
- `npm run dist:win`：成功。
- 新安装包：`release/迦勒底羁绊账本 Setup 0.1.0.exe`。
- 安装包 SHA-256：`7C79E11C0F5E287DECCEC4681CE6EBDAF9DEFA84FFC2196C0088CC49242F67DC`。
- 打包资源 SHA-256：`4CB3F7210EF9370E7E648E9C94E9EF0EF475F65CC7E84545659ED4F9DEAA44D0`。

## 尚未执行

未安装新安装包、未覆盖任何外部部署目录、未提交 Git，也未启动已打包程序进行人工点击验收。若需要进行其中任一动作，应先取得用户对该精确动作的授权。

## 2026-09-11：已授权覆盖部署目录

用户已明确授权覆盖 `E:\迦勒底羁绊账本`。实际部署目录经核对为 `E:\迦勒底羁绊账本\fgo-bond-ledger`，源目录为工作区的 `release/win-unpacked`。

覆盖前已确认主程序未运行；复制过程中未执行删除操作，仅以新构建产物覆盖同名文件。覆盖完成后，源目录与目标目录的哈希校验一致：

- `resources/app.asar`：`4CB3F7210EF9370E7E648E9C94E9EF0EF475F65CC7E84545659ED4F9DEAA44D0`
- `迦勒底羁绊账本.exe`：`045AD551E1B482A61DC7CC843CA045244807DD30E035AE3B56CF9AD7E4477C5D`

至此，`E:\迦勒底羁绊账本\fgo-bond-ledger` 已包含本轮四项修复对应的桌面程序文件。未执行安装程序，也未进行 Git 提交。
